export { default } from "next-auth/middleware";

// Uncomment and customize the matcher to protect specific routes
// export const config = {
//   matcher: ["/admin/:path*", "/payroll/:path*", "/ops/:path*"],
// };
